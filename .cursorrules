# FadeMemo AI Rules (Cursor)

> 本文件为 Cursor 专用项目规则，规则源来自 CONTRIBUTING.md。
> 修改规则时请同步更新 CONTRIBUTING.md。

## 项目概述

FadeMemo (记·忘) — 基于 Flutter 的跨平台间隔重复笔记应用。
技术栈：Flutter + Dart / Hive / Vite + React + TypeScript。

## 强制规则（最高优先级）

### 安全
- 禁止在代码、commit、PR、Issue 中写入密钥、Token、密码、API Key
- 发现敏感信息必须立即警告用户

### 分支保护
- 禁止直接向 main 或 develop 推送代码，所有改动必须通过 PR
- 禁止对 main/develop/release/hotfix 执行 force push
- 任何 git 操作前先执行 git branch --show-current 确认当前分支
- 创建新分支前必须先 git pull 同步目标分支最新代码

### 分支命名
- 功能分支：feature/<功能名>（从 develop 创建）
- 修复分支：bugfix/<问题描述>（从 develop 创建）
- 重构分支：refactor/<模块名>（从 develop 创建）
- 热修复分支：hotfix/<问题描述>（从 main 创建，先合 main 再合 develop）
- 发布分支：release/vX.Y.Z（从 develop 创建）

## Commit 规范（强制）

格式：`<type>(<scope>): <描述> 【<邮箱后四位>】`

- type: feat | fix | refactor | docs | test | style | chore
- scope: 可选模块名（canvas, editor, core, docs, ui, storage, sync）
- 描述：中文或英文，简洁明了
- 禁止: "update files", "fix bug", "changes", "update", "wip"
- 一次 Commit 只做一件事

## PR 规范

- PR 目标：日常功能 → develop，热修复 → main
- 标题使用 Conventional Commits 格式
- body 必须包含：变更说明、自测清单、关联任务
- 创建前确保本地分支已同步目标分支最新代码且无冲突
- 最低评审：main 需 1~2 人，develop 至少 1 人

## 冲突处理（强制）

- 禁止自动选择一方覆盖
- 必须逐个文件分析冲突，向用户解释差异，等待确认后再提交

## 代码生成

- 生成代码前必须先阅读现有代码，理解架构风格和命名约定
- 不得引入新依赖，除非用户明确要求
- 生成代码必须通过 flutter analyze 无 error
- 数据模型变更必须提供 Hive 迁移方案

## 确认机制

必须确认：推送代码、合并分支、删除分支、修改版本号、创建 PR、修改配置文件、引入新依赖
可跳过：读取文件、搜索代码、生成测试、格式化代码

## 人机协作

- 回复使用与用户相同的语言
- 报告问题时同时提供：问题描述、影响范围、建议方案
- 操作失败时立即停止、报告原因、提供回滚方案、等待指示

## 版本号

- 遵循 VERSIONING.md 语义化版本规范
- 不得自行修改 pubspec.yaml 版本号，除非用户明确指示
- 修改版本号时必须同步更新 CHANGELOG.md

## Dart/Flutter 命名

- 文件：snake_case.dart
- 类：PascalCase
- 函数/方法：camelCase
- 私有成员：_prefix
- 枚举值：camelCase

## 多 Agent 协作

- 主 Agent 负责任务调度和整体协调
- 子 Agent 不得越权操作非分配区域
- 审查 Agent 只能通过 PR 评论提供反馈
- 同级冲突以主 Agent 决策为准
